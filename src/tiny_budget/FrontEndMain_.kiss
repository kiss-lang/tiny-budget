(loadFrom "kiss-express" "src/Util.kiss")

(import js.Browser)

(Browser.window.addEventListener "load" ->{
    (let [p <#pTag>]
        (set p.innerHTML "Hello world from the frontend!")
        )
})